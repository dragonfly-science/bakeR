% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gateaux_job_tags.R
\name{gateaux_annotate_job}
\alias{gateaux_annotate_job}
\title{Annotate a gateaux/kahawai job with tags and/or metadata}
\usage{
gateaux_annotate_job(
  report_name,
  job_id,
  JWT,
  server = "flexion.larva.kahawai.net.nz",
  tags = NULL,
  metadata = NULL
)
}
\arguments{
\item{report_name}{The report code the job belongs to.}

\item{job_id}{The job ID to annotate.}

\item{JWT}{String: Authentication token.}

\item{server}{The server url to use. Defaults to flexion.larva.kahawai.net.nz}

\item{tags}{Optional named list/vector of searchable key-value pairs,
e.g. \code{list(state = "unreviewed", series = "XYZ123")}. Pass \code{list()}
to clear existing tags.}

\item{metadata}{Optional R list (or pre-formed json string) of arbitrary
metadata. Pass \code{list()} to clear existing metadata.}
}
\value{
The parsed JSON API response (invisibly).
}
\description{
Attaches tags (searchable key-value pairs, see \code{\link{gateaux_list_jobs}})
and/or metadata (arbitrary json) to a job. Tags and metadata are returned in
the job list API response. Passing an empty value (e.g. \code{tags = list()})
clears the corresponding field for the job.
}
\examples{
\dontrun{
gateaux_annotate_job(report_name = "MyReport",
                     job_id = "123456789",
                     JWT = JWT,
                     tags = list(state = "unreviewed", series = "XYZ123"),
                     metadata = list())
}
}
\author{
Dragonfly bakery
}

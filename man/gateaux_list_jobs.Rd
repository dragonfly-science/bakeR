% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gateaux_job_tags.R
\name{gateaux_list_jobs}
\alias{gateaux_list_jobs}
\title{List gateaux/kahawai jobs for a report}
\usage{
gateaux_list_jobs(
  report_name,
  JWT,
  server = "flexion.larva.kahawai.net.nz",
  page = 1,
  filter = NULL,
  tags = NULL
)
}
\arguments{
\item{report_name}{The report code to list jobs for.}

\item{JWT}{String: Authentication token.}

\item{server}{The server url to use. Defaults to flexion.larva.kahawai.net.nz}

\item{page}{Integer (or vector of integers): result page(s) to fetch. Results
from multiple pages are combined. Defaults to 1.}

\item{filter}{Optional string: limit jobs to those whose details contain this word.}

\item{tags}{Optional named list/vector of tag key-value pairs to match,
e.g. \code{list(author = "bob")}. Triggers a POST request.}
}
\value{
A data.frame of jobs (one row per job), most recent first. Job tags
and metadata are flattened into columns where present.
}
\description{
Lists jobs for a report, most recent first. Optionally filter by free-text
terms in the job details (\code{filter}) and/or by attached job tags
(\code{tags}, see \code{\link{gateaux_annotate_job}}). When \code{tags} are
supplied the call is sent as a POST request (as required by the API to match
tags), otherwise a GET request is used.
}
\examples{
\dontrun{
# all jobs for a report
gateaux_list_jobs(report_name = "MyReport", JWT = JWT)

# jobs tagged author=bob
gateaux_list_jobs(report_name = "MyReport", JWT = JWT, tags = list(author = "bob"))
}
}
\author{
Dragonfly bakery
}
